"""The three steps this project was built on, in the order they were worked through.

A random policy to fix the loop, tabular Q-learning where the table still fits in memory,
and a DQN written by hand beside the library's. Each is a module of its own, named for what
it does: nothing downstream imports them, and the notebook runs them in this order.
"""
